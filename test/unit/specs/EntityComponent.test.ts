import { Component } from '../../../src/nut/base/Component';
import { Entity } from '../../../src/nut/base/Entity';
import { Scene } from '../../../src/nut/base/Scene';

describe('EntityComponentScene', () => {
  describe('add', () => {
    it('One item.', () => {
      // const scene = new Scene();
      // collection.add(expected);
      // const actual = collection.getByComponent(GunsComponent)[0];
      // expect(actual).toBe(expected);
    });

    it('Multiple different items.', () => {
      // const collection = new EntitiesCollection();
      // const expected1 = new TankEntity();
      // const expected2 = new TankEntity();
      // collection.add(expected1);
      // collection.add(expected2);
      // const actual = collection.getByComponent(GunsComponent);
      // expect(actual.length).toBe(2);
      // expect(actual.indexOf(expected1)).toBeGreaterThan(-1);
      // expect(actual.indexOf(expected2)).toBeGreaterThan(-1);
    });
  });

  describe('remove', () => {
    it('By component', () => {
      //
    });
  });
});
